<%= link_to 'report a new claim', new_claim_path %>

<% @claims.each do |claim| %>
  	<%= render :partial => 'claim', :locals => {:claim => claim}%>
<% end %>

<%= link_to 'report a new claim', new_claim_path %>